import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Future: add allowed image domains for AI-generated bear illustrations
  // images: {
  //   remotePatterns: [{ hostname: 'your-image-cdn.com' }],
  // },
}

export default nextConfig
